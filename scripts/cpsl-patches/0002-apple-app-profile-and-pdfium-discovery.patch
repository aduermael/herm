diff --git a/core/Cargo.toml b/core/Cargo.toml
index 628ba14..3af3c6e 100644
--- a/core/Cargo.toml
+++ b/core/Cargo.toml
@@ -26,3 +26,3 @@ mod-http = ["dep:native-http"]
 mod-compress = ["dep:zip", "dep:tar", "dep:flate2", "dep:bzip2", "dep:xz2", "dep:sevenz-rust"]
-mod-doc = ["dep:calamine", "dep:rtf-parser", "dep:pulldown-cmark", "dep:native-webview-pdf", "dep:sha2", "dep:hex"]
+mod-doc = ["dep:zip", "dep:quick-xml", "dep:calamine", "dep:rtf-parser", "dep:pulldown-cmark", "dep:native-webview-pdf", "dep:sha2", "dep:hex"]
 mod-plot = ["dep:plotly"]
diff --git a/core/scripts/download-pdfium.sh b/core/scripts/download-pdfium.sh
index eb73dae..a6c71fe 100755
--- a/core/scripts/download-pdfium.sh
+++ b/core/scripts/download-pdfium.sh
@@ -3,3 +3,3 @@
 #
-# Usage: ./download-pdfium.sh [--version VERSION] [--output DIR]
+# Usage: ./download-pdfium.sh [--version VERSION] [--target TARGET] [--output DIR]
 #
@@ -20,2 +20,3 @@ DEFAULT_OUTPUT="$SCRIPT_DIR/../libs/pdfium"
 OUTPUT=""
+TARGET="${PDFIUM_TARGET:-}"
 while [[ $# -gt 0 ]]; do
@@ -23,2 +24,3 @@ while [[ $# -gt 0 ]]; do
     --version) PDFIUM_VERSION="$2"; shift 2 ;;
+    --target)  TARGET="$2"; shift 2 ;;
     --output)  OUTPUT="$2"; shift 2 ;;
@@ -62,4 +64,4 @@ detect_target() {
 }
-
+
-TARGET="$(detect_target)"
+TARGET="${TARGET:-$(detect_target)}"
 ASSET="pdfium-${TARGET}.tgz"
@@ -69,5 +71,6 @@ CACHE_FILE="$CACHE_DIR/$ASSET"
 case "$TARGET" in
-  mac-*)   LIB_NAME="libpdfium.dylib" ;;
-  linux-*) LIB_NAME="libpdfium.so" ;;
-  win-*)   LIB_NAME="pdfium.dll" ;;
+  ios-*|mac-*) LIB_NAME="libpdfium.dylib" ;;
+  linux-*)     LIB_NAME="libpdfium.so" ;;
+  win-*)       LIB_NAME="pdfium.dll" ;;
+  *)           echo "Unsupported PDFium target: $TARGET" >&2; exit 1 ;;
 esac
@@ -98,4 +101,4 @@ tar xzf "$CACHE_FILE" -C "$OUTPUT"
 tar xzf "$CACHE_FILE" -C "$OUTPUT"
-
+
-echo "PDFium ${PDFIUM_VERSION} ready: $OUTPUT/$LIB_NAME"
+echo "PDFium ${PDFIUM_VERSION} ready: $OUTPUT/lib/$LIB_NAME"
 echo ""
diff --git a/core/src/doc.rs b/core/src/doc.rs
index 5ff2355..ee40ffc 100644
--- a/core/src/doc.rs
+++ b/core/src/doc.rs
@@ -400,2 +400,3 @@ const DOC_RENDER_FILE_PARAMS: &[Param] = &[
 /// Static doc for structural-only mode (no vision callback).
+#[cfg(not(feature = "pdfium-render"))]
 pub(crate) static DOC_MOD_DOC: ModuleDoc = ModuleDoc {
@@ -434,2 +435,3 @@ pub(crate) static DOC_MOD_DOC: ModuleDoc = ModuleDoc {
 /// Static doc for vision-enabled mode (vision callback registered).
+#[cfg(not(feature = "pdfium-render"))]
 static DOC_MOD_DOC_VISION: ModuleDoc = ModuleDoc {
diff --git a/core/src/pdfium_engine.rs b/core/src/pdfium_engine.rs
index 04a805e..affa248 100644
--- a/core/src/pdfium_engine.rs
+++ b/core/src/pdfium_engine.rs
@@ -94,3 +94,3 @@ impl PdfiumEngine {
     fn platform_lib_name() -> &'static str {
-        #[cfg(target_os = "macos")]
+        #[cfg(any(target_os = "macos", target_os = "ios"))]
         {
@@ -106,3 +106,8 @@ impl PdfiumEngine {
         }
-        #[cfg(not(any(target_os = "macos", target_os = "linux", target_os = "windows")))]
+        #[cfg(not(any(
+            target_os = "macos",
+            target_os = "ios",
+            target_os = "linux",
+            target_os = "windows"
+        )))]
         {
diff --git a/core/src/sandbox.rs b/core/src/sandbox.rs
index 4f492f0..d3c4331 100644
--- a/core/src/sandbox.rs
+++ b/core/src/sandbox.rs
@@ -16,5 +16,7 @@ pub(crate) use doc::FS_DOC;
 pub(crate) use doc::{
-    arg_error, validate_args, FieldDoc, FnDoc, HelpMode, ModuleDoc, Param, ParamType, ReturnType,
+    arg_error, FieldDoc, FnDoc, HelpMode, ModuleDoc, Param, ParamType, ReturnType,
 };
-
+#[allow(unused_imports)]
+pub(crate) use doc::validate_args;
+
 mod errors;
diff --git a/core/src/sandbox/tests.rs b/core/src/sandbox/tests.rs
index 19ef9ec..044807d 100644
--- a/core/src/sandbox/tests.rs
+++ b/core/src/sandbox/tests.rs
@@ -1029,4 +1029,6 @@ fn all_module_docs() -> Vec<(&'static str, &'static ModuleDoc)> {
     docs.push(("compress", &crate::compress::COMPRESS_DOC));
-    #[cfg(feature = "mod-doc")]
+    #[cfg(all(feature = "mod-doc", not(feature = "pdfium-render")))]
     docs.push(("doc", &crate::doc::DOC_MOD_DOC));
+    #[cfg(all(feature = "mod-doc", feature = "pdfium-render"))]
+    docs.push(("doc", &crate::doc::DOC_MOD_DOC_PDFIUM));
     #[cfg(feature = "mod-http")]
diff --git a/docs/feature-flags.md b/docs/feature-flags.md
index a4b9b1e..5529e4d 100644
--- a/docs/feature-flags.md
+++ b/docs/feature-flags.md
@@ -86,2 +86,3 @@ and builds the dynamic library that Herm loads with `--cpsl`.
 | Herm demo minimum | `cargo build -p cpsl-ffi --release` | `fs`, `json`, `csv`, `http`, `ripgrep` |
+| Herm Apple app | `cargo build -p cpsl-ffi --release --no-default-features --features herm-apple-app` | minimum profile plus `yaml`, `xml`, `doc` + PDFium, `plot`, `numpy`, `random`, `fuzzy`, `phone`, `email`, `country`, `datetime`, `image`, `base64`, `fin`, `regex`, `html`, `url`, `qr` |
 | All core features | `cargo build -p cpsl-ffi --release --features all` | every `cpsl-core/all` feature listed above |
diff --git a/ffi/Cargo.toml b/ffi/Cargo.toml
index 927ada9..a9d71b8 100644
--- a/ffi/Cargo.toml
+++ b/ffi/Cargo.toml
@@ -19,2 +19,24 @@ herm-minimal = [
 ]
+herm-apple-app = [
+    "herm-minimal",
+    "cpsl-core/mod-yaml",
+    "cpsl-core/mod-xml",
+    "cpsl-core/mod-doc",
+    "cpsl-core/mod-plot",
+    "cpsl-core/mod-numpy",
+    "cpsl-core/mod-random",
+    "cpsl-core/mod-fuzzy",
+    "cpsl-core/mod-phone",
+    "cpsl-core/mod-email",
+    "cpsl-core/mod-country",
+    "cpsl-core/mod-datetime",
+    "cpsl-core/mod-image",
+    "cpsl-core/mod-base64",
+    "cpsl-core/mod-fin",
+    "cpsl-core/mod-regex",
+    "cpsl-core/mod-html",
+    "cpsl-core/mod-url",
+    "cpsl-core/mod-qr",
+    "pdfium-render",
+]
 pdfium-render = ["cpsl-core/pdfium-render"]
diff --git a/ffi/README.md b/ffi/README.md
index 3ee23a3..a31947c 100644
--- a/ffi/README.md
+++ b/ffi/README.md
@@ -16,2 +16,9 @@ cargo build -p cpsl-ffi --release
-
+
+The Herm Apple app profile adds the cross-platform Rust-backed utility modules
+used by the iOS/macOS app, plus document/PDF support:
+
+```sh
+cargo build -p cpsl-ffi --release --no-default-features --features herm-apple-app
+```
+
 To test Herm with every CPSL core feature compiled into the same library:
diff --git a/ffi/src/lib.rs b/ffi/src/lib.rs
index 42f01fe..b24d842 100644
--- a/ffi/src/lib.rs
+++ b/ffi/src/lib.rs
@@ -366,8 +366,14 @@ fn push_unique_path(paths: &mut Vec<PathBuf>, path: PathBuf) {
 fn discover_pdfium_from_base(base: &Path) -> Option<PdfiumEngine> {
-    let lib_dir = base.join("libs").join("pdfium").join("lib");
-    if lib_dir.is_dir() {
-        PdfiumEngine::from_path(lib_dir).ok()
-    } else {
-        None
+    for candidate in [
+        base.join("libs").join("pdfium").join("lib"),
+        base.to_path_buf(),
+        base.join("Frameworks"),
+    ] {
+        if candidate.exists() {
+            if let Ok(engine) = PdfiumEngine::from_path(&candidate) {
+                return Some(engine);
+            }
+        }
     }
+    None
 }
